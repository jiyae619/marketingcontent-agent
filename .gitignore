# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local secrets
.env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

node_modules
dist
dist-ssr
*.local

# Local backups
*.backup.js
*.backup.css
*.backup.html
style.backup.css
app.backup.js
index.backup.html

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*storybook.log
storybook-static

# Feedback DB (user voice samples — local only)
testing/results/feedback.db
testing/results/feedback.db-journal
testing/results/*.bak

# Golden set and its drift baseline are COMMITTED, not ignored: eval.gate_metrics
# (tools/bughunt/detectors.py) needs both to run in CI, which has no live model
# access to regenerate them. See tools/bughunt/detectors.py check_eval_quality().
# Rebuild locally with scripts/golden_set.py if the set needs to change.
